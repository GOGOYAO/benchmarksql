The test process for CockroachDB
# Create the tables and Add the indices 
./runSQL.sh props.pg sql.crdb/tableCreates.sql && ./runSQL.sh props.pg sql.crdb/indexCreates.sql
# Import Data
./runLoader.sh props.pg
# Run the benchmark
./runBenchmark.sh props.pg
